<%= form_for(@chord) do |f| %>
  <% if @chord.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@chord.errors.count, "error") %> prohibited this chord from being saved:</h2>

      <ul>
      <% @chord.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :contents %><br>
    <%= f.text_area :contents %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
